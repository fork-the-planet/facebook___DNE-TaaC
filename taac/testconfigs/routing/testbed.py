# (c) Meta Platforms, Inc. and affiliates. Confidential and proprietary.
# pyre-unsafe
"""Routing test DUT baseline definitions.

Home of the ``Testbed`` dataclass + all Testbed instances used by
routing testconfig factories. Per-role config bundles (peer group
names, route maps, communities) live in the sibling ``role_defaults.py``.

See ``README.md`` for the framework rules.

Skeleton — populated as Wave 1 migration diffs land.
"""
